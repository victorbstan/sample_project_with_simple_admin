<p id="notice"><%= notice %></p>

<%= link_to 'Back', posts_path %>

<p>
  <strong>Title:</strong>
  <%= @post.title %>
</p>

<p>
  <strong>Body:</strong>
  <%= @post.body %>
</p>

<% @post.comments.each do |comment| %>
<%= render 'comments/show_embedded', { comment: comment } %>
<% end %>

<%= render 'comments/form' %>

<%= console %>