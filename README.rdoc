== README

This is a sample application that makes use of SimpleAdmin Rails Engine (gem) and is intended for demonstration purposes only.

Clone this application and run `rails s`. Take a look at what it does; go to `/admin/` path to see SimpleAdmin in action.

Take a look at the `/config/initializers/simple_admin.rb` for configuration example.
